<nav class="navbar navbar-expand-sm navbar-dark bg-light-blue fixed-top">
    <div class="container">
        <a href="{{ path('welcome') }}" class="navbar-brand">The Perfect Diet</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="{{ path('welcome') }}" class="nav-link">Home</a>
                </li>
{% if app.user %}
                <li class="nav-item">
                    <a href="{{ path('home') }}" class="nav-link">Gestion de Régime</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('home2') }}" class="nav-link">Gestion de Plat</a>
                </li>
{% endif %}
                {% if not app.user %}
                    <li class="nav-item">
                        <a href="{{ path('security_login')}}" class="btn btn-secondary btn-lg active">Connexion</a>
                    </li>
                {% else %}
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {{ app.user.username }}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="userDropdown">
                            <a href="{{ path('security_logout')}}" class="dropdown-item deconnex-link">Déconnexion</a>
                        </div>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<style>
    .bg-light-blue {
        background-color: #7C93C3; /* Light blue color code */
    }

    .navbar {
        z-index: 1000; /* Adjust this value based on your layout */
    }

    .navbar-brand {
        font-size: 1.5rem; /* Adjust the font size as needed */
        font-weight: bold; /* Make the brand text bold */
    }

    .nav-link {
        font-size: 1.1rem; /* Adjust the font size as needed */
        margin-right: 15px; /* Add margin between nav items */
    }

    /* Styling for Connexion link */
    .connex-link {
        color: #fff; /* White text for Connexion link */
        font-weight: bold; /* Make Connexion link bold */
    }

    /* Styling for Déconnexion link */
    .deconnex-link {
        color: #ff0000; /* Red text for Déconnexion link */
        font-weight: bold; /* Make Déconnexion link bold */
    }

    .dropdown-menu {
        background-color: #7C93C3; /* Light blue color for the dropdown menu */
    }

    .dropdown-item {
        color: #fff; /* White text for dropdown items */
    }
</style>
